# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure gnutls-utils is installed
  ansible.builtin.package:
    name: "gnutls-utils"
    state: present
