# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low









- name: Find /etc/sudoers.d/ file(s)
  ansible.builtin.command: 'find -P /etc/sudoers.d/ -maxdepth 0 -perm /u+s,g+ws,o+xwrt  -type d '
  register: files_found
  changed_when: False
  failed_when: False
  check_mode: no

- name: Set permissions for /etc/sudoers.d/ file(s)
  ansible.builtin.file:
    path: "{{ item }}"
    mode: "u-s,g-ws,o-xwrt"
    state: "directory"
  with_items:
    - "{{ files_found.stdout_lines }}"


