# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure ntp is installed
  ansible.builtin.package:
    name: "ntp"
    state: present
