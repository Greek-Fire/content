# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libpam-runtime is installed
  ansible.builtin.package:
    name: "libpam-runtime"
    state: present
