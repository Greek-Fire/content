# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure gnome-software is installed
  ansible.builtin.package:
    name: "gnome-software"
    state: present
