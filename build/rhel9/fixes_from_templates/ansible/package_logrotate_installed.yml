# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure logrotate is installed
  ansible.builtin.package:
    name: "logrotate"
    state: present
