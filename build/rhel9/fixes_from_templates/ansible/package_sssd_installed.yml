# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure sssd is installed
  ansible.builtin.package:
    name: "sssd"
    state: present
