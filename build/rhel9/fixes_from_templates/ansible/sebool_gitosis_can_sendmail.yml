# platform = multi_platform_almalinux,multi_platform_fedora,multi_platform_ol,multi_platform_rhel,multi_platform_rhv,SUSE Linux Enterprise 15,SUSE Linux Enterprise 16
# reboot = false
# strategy = enable
# complexity = low
# disruption = low





- name: "Disable the gitosis_can_sendmail SELinux Boolean - Ensure python3-libsemanage Installed"
  ansible.builtin.package:
    name: "python3-libsemanage"
    state: present




- (xccdf-var var_gitosis_can_sendmail)
- name: "Disable the gitosis_can_sendmail SELinux Boolean - Set SELinux Boolean gitosis_can_sendmail Accordingly"
  ansible.posix.seboolean:
    name: gitosis_can_sendmail
    state: "{{ var_gitosis_can_sendmail }}"
    persistent: yes
