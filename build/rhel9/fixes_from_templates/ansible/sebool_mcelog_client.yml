# platform = multi_platform_almalinux,multi_platform_fedora,multi_platform_ol,multi_platform_rhel,multi_platform_rhv,SUSE Linux Enterprise 15,SUSE Linux Enterprise 16
# reboot = false
# strategy = enable
# complexity = low
# disruption = low





- name: "Disable the mcelog_client SELinux Boolean - Ensure python3-libsemanage Installed"
  ansible.builtin.package:
    name: "python3-libsemanage"
    state: present




- (xccdf-var var_mcelog_client)
- name: "Disable the mcelog_client SELinux Boolean - Set SELinux Boolean mcelog_client Accordingly"
  ansible.posix.seboolean:
    name: mcelog_client
    state: "{{ var_mcelog_client }}"
    persistent: yes
