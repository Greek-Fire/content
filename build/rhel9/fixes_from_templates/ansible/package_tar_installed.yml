# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure tar is installed
  ansible.builtin.package:
    name: "tar"
    state: present
