# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall abrt-plugin-rhtsupport Package: Ensure abrt-plugin-rhtsupport is removed"
  ansible.builtin.package:
    name: "abrt-plugin-rhtsupport"
    state: absent
