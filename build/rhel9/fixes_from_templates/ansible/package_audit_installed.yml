# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure audit is installed
  ansible.builtin.package:
    name: "audit"
    state: present
