# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall kea Package: Ensure kea is removed"
  ansible.builtin.package:
    name: "kea"
    state: absent
