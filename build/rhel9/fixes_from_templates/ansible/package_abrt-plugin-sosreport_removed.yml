# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall abrt-plugin-sosreport Package: Ensure abrt-plugin-sosreport is removed"
  ansible.builtin.package:
    name: "abrt-plugin-sosreport"
    state: absent
