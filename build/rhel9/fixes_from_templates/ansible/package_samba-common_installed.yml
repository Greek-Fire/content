# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure samba-common is installed
  ansible.builtin.package:
    name: "samba-common"
    state: present
