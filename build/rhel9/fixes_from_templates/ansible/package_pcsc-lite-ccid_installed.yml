# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure pcsc-lite-ccid is installed
  ansible.builtin.package:
    name: "pcsc-lite-ccid"
    state: present
