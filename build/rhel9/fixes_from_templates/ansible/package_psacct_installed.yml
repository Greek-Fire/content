# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure psacct is installed
  ansible.builtin.package:
    name: "psacct"
    state: present
