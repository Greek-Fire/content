# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure fapolicyd is installed
  ansible.builtin.package:
    name: "fapolicyd"
    state: present
