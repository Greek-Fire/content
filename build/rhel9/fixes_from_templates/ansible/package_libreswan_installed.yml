# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libreswan is installed
  ansible.builtin.package:
    name: "libreswan"
    state: present
