# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure audit-libs is installed
  ansible.builtin.package:
    name: "audit-libs"
    state: present
