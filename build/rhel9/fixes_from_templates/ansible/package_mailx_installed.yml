# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure mailx is installed
  ansible.builtin.package:
    name: "mailx"
    state: present
