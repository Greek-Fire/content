# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure cronie is installed
  ansible.builtin.package:
    name: "cronie"
    state: present
