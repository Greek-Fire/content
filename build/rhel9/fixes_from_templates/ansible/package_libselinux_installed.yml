# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libselinux is installed
  ansible.builtin.package:
    name: "libselinux"
    state: present
