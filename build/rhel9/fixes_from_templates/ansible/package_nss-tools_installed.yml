# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure nss-tools is installed
  ansible.builtin.package:
    name: "nss-tools"
    state: present
