# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Remove the FreeRadius Server Package: Ensure freeradius is removed"
  ansible.builtin.package:
    name: "freeradius"
    state: absent
