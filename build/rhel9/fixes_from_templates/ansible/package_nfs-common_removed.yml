# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall nfs-common Package: Ensure nfs-common is removed"
  ansible.builtin.package:
    name: "nfs-common"
    state: absent
