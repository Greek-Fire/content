# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low









- name: Find /var/log/apt/ file(s)
  ansible.builtin.command: 'find -P /var/log/apt/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex "^.*$"'
  register: files_found
  changed_when: False
  failed_when: False
  check_mode: no

- name: Set permissions for /var/log/apt/ file(s)
  ansible.builtin.file:
    path: "{{ item }}"
    mode: "u-xs,g-xws,o-xwt"
    state: "file"
  with_items:
    - "{{ files_found.stdout_lines }}"


