# platform = multi_platform_all
# reboot = true
# strategy = disable
# complexity = low
# disruption = medium
- (xccdf-var sysctl_net_ipv6_conf_all_accept_ra_pinfo_value)


- name: Configure Accepting Prefix Information in Router Advertisements on All IPv6 Interfaces - Set fact for sysctl paths
  ansible.builtin.set_fact:

    sysctl_paths:
      - "/etc/sysctl.d/"
      - "/run/sysctl.d/"
      - "/usr/local/lib/sysctl.d/"



- name: Configure Accepting Prefix Information in Router Advertisements on All IPv6 Interfaces - Find all files that contain net.ipv6.conf.all.accept_ra_pinfo
  ansible.builtin.shell:
    cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_ra_pinfo\s*=\s*.*$'
  register: find_all_values
  check_mode: false
  changed_when: false
  failed_when: false

- name: Configure Accepting Prefix Information in Router Advertisements on All IPv6 Interfaces - Find all files that set net.ipv6.conf.all.accept_ra_pinfo to correct value
  ansible.builtin.shell:
    cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_ra_pinfo\s*=\s*{{ sysctl_net_ipv6_conf_all_accept_ra_pinfo_value }}$'
  register: find_correct_value
  check_mode: false
  changed_when: false
  failed_when: false

- name: Configure Accepting Prefix Information in Router Advertisements on All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_ra_pinfo from config files
  ansible.builtin.replace:
    path: '{{ item | split(":") | first }}'
    regexp: '^[\s]*net.ipv6.conf.all.accept_ra_pinfo'
    replace: '#net.ipv6.conf.all.accept_ra_pinfo'
  loop: '{{ find_all_values.stdout_lines }}'
  when: find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines | length




- name: Configure Accepting Prefix Information in Router Advertisements on All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_ra_pinfo from /etc/sysctl.conf
  ansible.builtin.replace:
    path: "{{ item }}"
    regexp: '^[\s]*net.ipv6.conf.all.accept_ra_pinfo'
    replace: '#net.ipv6.conf.all.accept_ra_pinfo'
  with_fileglob:
    - "/etc/sysctl.conf"

- name: Configure Accepting Prefix Information in Router Advertisements on All IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_ra_pinfo is set
  ansible.posix.sysctl:
    name: "net.ipv6.conf.all.accept_ra_pinfo"
    value: "{{ sysctl_net_ipv6_conf_all_accept_ra_pinfo_value }}"

    sysctl_file: "/etc/sysctl.d/net_ipv6_conf_all_accept_ra_pinfo.conf"

    state: present
    reload: yes