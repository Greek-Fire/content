# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure tcp_wrappers is installed
  ansible.builtin.package:
    name: "tcp_wrappers"
    state: present
