# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure ufw is installed
  ansible.builtin.package:
    name: "ufw"
    state: present
