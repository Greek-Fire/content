# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low









- name: Find /boot/grub2/ file(s)
  ansible.builtin.command: 'find -P /boot/grub2/ -maxdepth 1 -perm /u+xs,g+xwrs,o+xwrt  -type f -regextype posix-extended -regex "^.*$"'
  register: files_found
  changed_when: False
  failed_when: False
  check_mode: no

- name: Set permissions for /boot/grub2/ file(s)
  ansible.builtin.file:
    path: "{{ item }}"
    mode: "u-xs,g-xwrs,o-xwrt"
    state: "file"
  with_items:
    - "{{ files_found.stdout_lines }}"


