# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure usbguard is installed
  ansible.builtin.package:
    name: "usbguard"
    state: present
