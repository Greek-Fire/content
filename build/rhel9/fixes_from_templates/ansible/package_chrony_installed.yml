# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure chrony is installed
  ansible.builtin.package:
    name: "chrony"
    state: present
