# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure apparmor-utils is installed
  ansible.builtin.package:
    name: "apparmor-utils"
    state: present
