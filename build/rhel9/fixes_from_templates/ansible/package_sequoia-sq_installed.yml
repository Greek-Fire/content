# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure sequoia-sq is installed
  ansible.builtin.package:
    name: "sequoia-sq"
    state: present
