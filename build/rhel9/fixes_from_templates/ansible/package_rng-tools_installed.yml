# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure rng-tools is installed
  ansible.builtin.package:
    name: "rng-tools"
    state: present
