# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure binutils is installed
  ansible.builtin.package:
    name: "binutils"
    state: present
