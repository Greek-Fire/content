# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure glibc is installed
  ansible.builtin.package:
    name: "glibc"
    state: present
