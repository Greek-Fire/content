# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure screen is installed
  ansible.builtin.package:
    name: "screen"
    state: present
