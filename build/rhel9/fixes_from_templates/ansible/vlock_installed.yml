# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure kbd is installed
  ansible.builtin.package:
    name: "kbd"
    state: present
