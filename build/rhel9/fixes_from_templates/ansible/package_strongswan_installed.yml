# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure strongswan is installed
  ansible.builtin.package:
    name: "strongswan"
    state: present
