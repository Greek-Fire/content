# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall setroubleshoot-server Package: Ensure setroubleshoot-server is removed"
  ansible.builtin.package:
    name: "setroubleshoot-server"
    state: absent
