# platform = multi_platform_all
# reboot = true
# strategy = disable
# complexity = low
# disruption = medium

- name: Limit sampling frequency of the Perf system - Set fact for sysctl paths
  ansible.builtin.set_fact:

    sysctl_paths:
      - "/etc/sysctl.d/"
      - "/run/sysctl.d/"
      - "/usr/local/lib/sysctl.d/"



- name: Limit sampling frequency of the Perf system - Find all files that contain kernel.perf_event_max_sample_rate
  ansible.builtin.shell:
    cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.perf_event_max_sample_rate\s*=\s*.*$'
  register: find_all_values
  check_mode: false
  changed_when: false
  failed_when: false

- name: Limit sampling frequency of the Perf system - Find all files that set kernel.perf_event_max_sample_rate to correct value
  ansible.builtin.shell:
    cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.perf_event_max_sample_rate\s*=\s*1$'
  register: find_correct_value
  check_mode: false
  changed_when: false
  failed_when: false

- name: Limit sampling frequency of the Perf system - Comment out any occurrences of kernel.perf_event_max_sample_rate from config files
  ansible.builtin.replace:
    path: '{{ item | split(":") | first }}'
    regexp: '^[\s]*kernel.perf_event_max_sample_rate'
    replace: '#kernel.perf_event_max_sample_rate'
  loop: '{{ find_all_values.stdout_lines }}'
  when: find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines | length




- name: Limit sampling frequency of the Perf system - Comment out any occurrences of kernel.perf_event_max_sample_rate from /etc/sysctl.conf
  ansible.builtin.replace:
    path: "{{ item }}"
    regexp: '^[\s]*kernel.perf_event_max_sample_rate'
    replace: '#kernel.perf_event_max_sample_rate'
  with_fileglob:
    - "/etc/sysctl.conf"

- name: Limit sampling frequency of the Perf system - Ensure sysctl kernel.perf_event_max_sample_rate is set to 1
  ansible.posix.sysctl:
    name: "kernel.perf_event_max_sample_rate"
    value: "1"

    sysctl_file: "/etc/sysctl.d/kernel_perf_event_max_sample_rate.conf"

    state: present
    reload: yes