# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall geolite2-city Package: Ensure geolite2-city is removed"
  ansible.builtin.package:
    name: "geolite2-city"
    state: absent
