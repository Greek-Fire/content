# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure rsyslog-gnutls is installed
  ansible.builtin.package:
    name: "rsyslog-gnutls"
    state: present
