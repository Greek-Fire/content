# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure apparmor is installed
  ansible.builtin.package:
    name: "apparmor"
    state: present
