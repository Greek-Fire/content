# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Package "prelink" Must not be Installed: Ensure prelink is removed"
  ansible.builtin.package:
    name: "prelink"
    state: absent
