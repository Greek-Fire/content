# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure vsftpd is installed
  ansible.builtin.package:
    name: "vsftpd"
    state: present
