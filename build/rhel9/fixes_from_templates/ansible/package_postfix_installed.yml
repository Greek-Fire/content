# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure postfix is installed
  ansible.builtin.package:
    name: "postfix"
    state: present
