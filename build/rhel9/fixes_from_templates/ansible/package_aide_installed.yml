# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure aide is installed
  ansible.builtin.package:
    name: "aide"
    state: present
