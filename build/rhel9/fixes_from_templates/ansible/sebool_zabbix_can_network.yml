# platform = multi_platform_almalinux,multi_platform_fedora,multi_platform_ol,multi_platform_rhel,multi_platform_rhv,SUSE Linux Enterprise 15,SUSE Linux Enterprise 16
# reboot = false
# strategy = enable
# complexity = low
# disruption = low





- name: "Disable the zabbix_can_network SELinux Boolean - Ensure python3-libsemanage Installed"
  ansible.builtin.package:
    name: "python3-libsemanage"
    state: present




- (xccdf-var var_zabbix_can_network)
- name: "Disable the zabbix_can_network SELinux Boolean - Set SELinux Boolean zabbix_can_network Accordingly"
  ansible.posix.seboolean:
    name: zabbix_can_network
    state: "{{ var_zabbix_can_network }}"
    persistent: yes
