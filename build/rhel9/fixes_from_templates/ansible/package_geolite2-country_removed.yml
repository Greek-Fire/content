# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall geolite2-country Package: Ensure geolite2-country is removed"
  ansible.builtin.package:
    name: "geolite2-country"
    state: absent
