# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure rear is installed
  ansible.builtin.package:
    name: "rear"
    state: present
