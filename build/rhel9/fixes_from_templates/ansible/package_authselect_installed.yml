# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure authselect is installed
  ansible.builtin.package:
    name: "authselect"
    state: present
