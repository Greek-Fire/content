# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall the inet-based telnet server: Ensure inetutils-telnetd is removed"
  ansible.builtin.package:
    name: "inetutils-telnetd"
    state: absent
