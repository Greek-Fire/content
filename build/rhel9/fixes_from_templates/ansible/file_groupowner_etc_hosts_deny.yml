# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low
- name: Set the file_groupowner_etc_hosts_deny_newgroup variable if represented by gid
  ansible.builtin.set_fact:
    file_groupowner_etc_hosts_deny_newgroup: "0"

- name: Test for existence /etc/hosts.deny
  ansible.builtin.stat:
    path: "/etc/hosts.deny"
  register: file_exists

- name: Ensure group owner on /etc/hosts.deny
  ansible.builtin.file:
    path: "/etc/hosts.deny"
    follow: false
    group: "{{ file_groupowner_etc_hosts_deny_newgroup }}"
  when: file_exists.stat is defined and file_exists.stat.exists


