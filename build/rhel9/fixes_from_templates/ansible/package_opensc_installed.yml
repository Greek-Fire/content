# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure opensc is installed
  ansible.builtin.package:
    name: "opensc"
    state: present
