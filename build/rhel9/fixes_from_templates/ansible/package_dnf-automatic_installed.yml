# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure dnf-automatic is installed
  ansible.builtin.package:
    name: "dnf-automatic"
    state: present
