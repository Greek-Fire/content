# platform = multi_platform_almalinux,multi_platform_fedora,multi_platform_ol,multi_platform_rhel,multi_platform_rhv,SUSE Linux Enterprise 15,SUSE Linux Enterprise 16
# reboot = false
# strategy = enable
# complexity = low
# disruption = low





- name: "Disable the nis_enabled SELinux Boolean - Ensure python3-libsemanage Installed"
  ansible.builtin.package:
    name: "python3-libsemanage"
    state: present




- (xccdf-var var_nis_enabled)
- name: "Disable the nis_enabled SELinux Boolean - Set SELinux Boolean nis_enabled Accordingly"
  ansible.posix.seboolean:
    name: nis_enabled
    state: "{{ var_nis_enabled }}"
    persistent: yes
