# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low


- name: "Uninstall abrt-libs Package: Ensure abrt-libs is removed"
  ansible.builtin.package:
    name: "abrt-libs"
    state: absent
