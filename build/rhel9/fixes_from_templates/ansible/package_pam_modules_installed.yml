# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libpam-modules is installed
  ansible.builtin.package:
    name: "libpam-modules"
    state: present
