# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure firewalld is installed
  ansible.builtin.package:
    name: "firewalld"
    state: present
