# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure iptables is installed
  ansible.builtin.package:
    name: "iptables"
    state: present
