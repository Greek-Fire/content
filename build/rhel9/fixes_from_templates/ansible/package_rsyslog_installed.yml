# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure rsyslog is installed
  ansible.builtin.package:
    name: "rsyslog"
    state: present
