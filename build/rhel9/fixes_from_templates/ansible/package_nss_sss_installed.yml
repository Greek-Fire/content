# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libnss-sss is installed
  ansible.builtin.package:
    name: "libnss-sss"
    state: present
