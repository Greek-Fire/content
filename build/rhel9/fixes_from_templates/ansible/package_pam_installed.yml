# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure pam is installed
  ansible.builtin.package:
    name: "pam"
    state: present
