# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure uuidd is installed
  ansible.builtin.package:
    name: "uuidd"
    state: present
